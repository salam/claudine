## Agent Integration

Create a `CLAUDINE.AGENTS.md` file in your workspace root to let Claude Code agents interact with the board.

Agents can move conversations between columns, update descriptions, and report status changes automatically as they work.

Reference the file from your project's `CLAUDE.md` to activate it.
