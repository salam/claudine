v1.1.2: Agent status bar, BUG7/BUG10 fixes, about dialog redesign, standalone settings persistence

- Replace one-time agent integration popup with a persistent status bar
  item that shows missing/unreferenced/ok state and auto-refreshes via
  file watchers on CLAUDINE.AGENTS.md, AGENTS.md, CLAUDE.md
- BUG7: Anchor rate-limit reset-time computation to the message timestamp
  (not parse-time) and expire stale limits whose reset time is in the past
  or older than 6 hours
- BUG10: Prevent trailing agent output from bumping manually-closed tasks
  back into active columns when no agent is genuinely active
- Standalone: toggleSummarization now persists via setConfig(); updateSetting
  handles all allowed config keys (not just secrets)
- Redesigned About dialog with SVG logo, gradient title, structured nav
  links (website, GitHub, Marketplace, Sponsor), backdrop blur
- Summarization toolbar icon switches between filled/outlined SVG based on
  enabled state
- Quick idea input upgraded from single-line <input> to auto-resizing
  <textarea> with dynamic height
- TaskCard: moved Genesis badge to a banner row, conditionally render
  meta-row only when content exists, removed standalone Respond button
- Build: build-vsix.sh uses proper semver comparison and delegates
  packaging to tools/package-vsix.sh
- Version bump 1.1.0 -> 1.1.2
- New tests, i18n strings, website favicon/logo refresh
