fix: route Codex conversations to correct editor commands (BUG17)

Clicking a Codex task card no longer opens an empty Claude Code tab.
KanbanViewProvider now resolves the correct IEditorCommands based on
the conversation's provider field. CodexEditorCommands opens the
session JSONL file as a fallback until the Codex extension exposes
public commands.
