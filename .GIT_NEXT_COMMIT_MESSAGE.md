feat: short task IDs and inline /claudine slash commands

Add human-friendly short IDs (T-1, T-2, ...) to every conversation,
displayed on task cards and usable for referencing tasks.

Add inline slash command detection: typing /claudine done, /claudine
archive, /claudine category bug, etc. in a Claude Code conversation
is detected from the JSONL file and routed through the existing
CommandProcessor pipeline. Commands can target other tasks by short ID
(e.g. /claudine done T-3).

New files:
- src/services/InlineCommandRouter.ts
- src/test/ShortId.test.ts
- src/test/InlineCommandDetection.test.ts
- src/test/InlineCommandRouter.test.ts

Modified:
- src/types/index.ts — InlineCommand types, shortId + pendingCommands on Conversation
- webview/src/lib/vscode.ts — shortId on webview Conversation
- src/services/StateManager.ts — counter, assignShortId, getConversationByShortId
- src/services/CommandProcessor.ts — short ID lookup, executeCommand now public
- src/providers/ConversationParser.ts — detectInlineCommands, pendingCommands
- src/providers/ClaudeCodeWatcher.ts — InlineCommandRouter integration
- src/extension.ts, standalone/server.ts — wiring
- webview/src/components/TaskCard.svelte — short ID display + CSS
