Show version number in About popup

Injects the extension version (from package.json) into the webview
via window.__CLAUDINE_VERSION__ and displays it in the About dialog,
for both VS Code and standalone modes.
