Add text-based question detection for "?" badge

Extends hasRecentQuestion() to also check if the last non-empty line
of the last assistant message ends with "?", not just AskUserQuestion
tool uses. Tasks with text-ending questions now show the "?" badge
and move to "Needs Input".
